global:
  message: Welcome to Global Microservices Environment
logging:
  level:
   root: INFO
management:
   endpoints:
     web:
       exposure:
         inlcude: health, info,refresh
    endpoint:
      health:
         show-details: always
  
